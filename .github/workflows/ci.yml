name: CI
run-name: CI [${{ github.head_ref || github.ref_name }}]

on:
  push:
    branches: [ 'main' ]
  pull_request:
    branches: [ 'main' ]

concurrency:
  group: ${{ github.workflow }}-${{ github.head_ref || github.ref_name }}
  cancel-in-progress: true

jobs:
  rust:
    name: Rust Coverage
    runs-on: ubuntu-latest
    steps:
      - uses: actions/checkout@v4
        with:
          fetch-depth: 0

      - name: Install cargo-llvm-cov
        run: cargo install cargo-llvm-cov

      - name: Test Rust + Coverage
        working-directory: ./worker-wasm/core
        run: cargo llvm-cov --workspace --lcov --output-path rust-lcov.info

      - uses: actions/upload-artifact@v4
        with:
          name: rust-coverage
          path: worker-wasm/core/rust-lcov.info

  js:
    name: JS Coverage
    runs-on: ubuntu-latest
    steps:
      - uses: actions/checkout@v4
        with:
          fetch-depth: 0

      - name: Use Node.js
        uses: actions/setup-node@v3

      - name: Install wasm-pack
        run: curl https://rustwasm.github.io/wasm-pack/installer/init.sh -sSf | sh

      - name: Install dependencies
        run: yarn install --immutable

      - name: Build WASM
        run: wasm-pack build
        working-directory: ./worker-wasm

      - name: Lint
        run: yarn lint

      - name: Test
        run: yarn test:cover

      - uses: actions/upload-artifact@v4
        with:
          name: js-coverage
          path: coverage/lcov.info

  build:
    name: Build Extension
    runs-on: ubuntu-latest
    steps:
      - uses: actions/checkout@v4
        with:
          fetch-depth: 0

      - name: Install wasm-pack
        run: curl https://rustwasm.github.io/wasm-pack/installer/init.sh -sSf | sh

      - name: Install dependencies
        run: yarn install --immutable

      - name: Build WASM
        run: wasm-pack build
        working-directory: ./worker-wasm

      - name: Build
        run: make build

  analyze:
    name: Analyze with SonarCloud
    runs-on: ubuntu-latest
    needs: [build, rust, js]
    steps:
      - uses: actions/checkout@v4
        with:
          fetch-depth: 0

      - uses: actions/download-artifact@v4
        with:
          name: rust-coverage
          path: ./worker-wasm/core

      - name: Display structure of downloaded files
        run: ls -R ./worker-wasm/core

      - uses: actions/download-artifact@v4
        with:
          name: js-coverage
          path: ./coverage

      - name: Display structure of downloaded files
        run: ls -R ./coverage

      - name: SonarCloud Scan
        uses: SonarSource/sonarqube-scan-action@master
        env:
          GITHUB_TOKEN: ${{ secrets.GITHUB_TOKEN }}
          SONAR_TOKEN: ${{ secrets.SONAR_TOKEN }}
